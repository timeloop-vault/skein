// Plan card body. D1 placeholder — the per-harness grouped plan rows
// with inferred-now for opencode (handover §5.2) land in D4.

export const PlanCardBody = () => (
	<div className="lc-empty">
		<div className="lc-empty-inner">
			<div className="big">·</div>
			no plan items yet — agents will populate this as they work
		</div>
	</div>
);

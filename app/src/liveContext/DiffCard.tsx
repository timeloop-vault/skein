// Diff card body. D1 placeholder — the real Monaco diff with
// auto-focus + multi-harness flicker (handover §5.1) lands in D3.

export const DiffCardBody = () => (
	<div className="lc-empty">
		<div className="lc-empty-inner">
			<div className="big">◇</div>
			when an agent edits a file, its diff appears here
		</div>
	</div>
);
